<!DOCTYPE html>
<html lang="en">

<head>
  <%- include("partials/head") %>
  <link rel="stylesheet" href="/css/navbar.css">
  <link rel="stylesheet" href="/css/home.css">
</head>

<body>
  <%- include("partials/navbar", { user: user }) %>

  <div class="container mt-5">
    <h2 class="text-center text-light fw-bold mb-4">Shorten Your URL</h2>

    <form action="/" method="post" class="card p-4 bg-dark text-light shadow-lg rounded">
      <label class="form-label fw-semibold">Enter URL:</label>
      <input type="text" class="form-control mb-3" name="url" required>

      <button class="btn btn-primary w-100 fw-bold">Generate Short URL</button>
    </form>

    <% if(shortUrl){ %>
      <div class="alert alert-info text-center mt-4">
        Short URL:  
        <a href="<%= shortUrl %>" target="_blank" class="text-primary fw-semibold">
          <%= shortUrl %>
        </a>
      </div>
    <% } %>

  </div>
</body>

</html>
