<!DOCTYPE html>
<html lang="en">
<%- include("partials/head", { title: "Home - ShortURL", pageCss: "home.css" }) %>

<body>

    <nav class="navbar">
        <div class="nav-left">
            <a href="/">ShortURL</a>
        </div>

        <div class="nav-right">
            <button id="themeToggle" class="toggle-btn">🌙</button>

            <% if (user) { %>
                <a href="/history">History</a>
                <a href="/logout">Logout</a>
            <% } else { %>
                <a href="/login">Login</a>
                <a href="/signup">Signup</a>
            <% } %>
        </div>
    </nav>

    <main class="home-container">

        <h1 class="title">Shorten Your URL</h1>

        <form action="/posturl" method="POST" class="url-form">
            <input type="text" name="longUrl" placeholder="Enter long URL..." required />
            <button type="submit">Shorten</button>
        </form>

        <% if (shortUrl) { %>
            <div class="result-box">
                <p>Your short URL:</p>
                <a href="<%= shortUrl %>" target="_blank"><%= shortUrl %></a>
            </div>
        <% } %>

    </main>

</body>
</html>
